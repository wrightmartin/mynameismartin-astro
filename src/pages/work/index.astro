---
/*
  Work Listing Page
  -----------------
  Displays all portfolio projects in a grid, sorted by their order number.
  Each project is rendered as a ProjectCard component.
  A client logos section sits at the bottom.
*/

import Page from '../../layouts/Page.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import ClientLogos from '../../components/ClientLogos.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('work');
const sorted = projects.sort((a, b) => a.data.order - b.data.order);
---

<Page title="Work" description="Selected projects showing how I design complex products and services under real-world constraints.">

  <section class="page-section full-bleed bg-white">
    <div class="full-bleed-inner">
      <div class="page-header mb-0">
        <h1>Selected work</h1>
        <p>Projects chosen to show how I design complex products and services under real-world constraints.</p>
      </div>
    </div>
  </section>
  <section class="page-section">
    <div class="card-grid">
      {sorted.map((project) => (
        <ProjectCard
          title={project.data.cardTitle || project.data.title}
          slug={project.data.slug}
          subtitle={project.data.subtitle}
          thumbnail={project.data.thumbnail}
        />
      ))}
    </div>
  </section>

  <ClientLogos />

</Page>
